<view class="page {{currentTheme}}">
  <!-- 顶部：搜索 + 主题切换 -->
  <view class="header">
    <view class="search-bar glass" bindtap="goToSearch">
      <view class="search-icon"></view>
      <text class="search-text">搜索笑话</text>
    </view>
    
    <view class="theme-btn glass" bindtap="toggleTheme">
      <text class="theme-icon">{{themeIcon}}</text>
    </view>
  </view>

  <!-- 分类 -->
  <scroll-view class="categories" scroll-x show-scrollbar="{{false}}">
    <view 
      class="cat-item {{currentCategory === item.name ? 'active' : ''}}"
      wx:for="{{categories}}"
      wx:key="name"
      bindtap="switchCategory"
      data-category="{{item.name}}"
    >
      <view class="cat-dot" style="background: {{item.color}}"></view>
      <text class="cat-name">{{item.name}}</text>
      <text class="cat-count" wx:if="{{item.count > 0}}">{{item.count}}</text>
    </view>
  </scroll-view>

  <!-- 新鲜未看 -->
  <view class="section" wx:if="{{todayJokes.length > 0}}">
    <view class="section-header">
      <view class="section-title-wrap">
        <view class="title-bar"></view>
        <text class="section-title">新鲜{{todayJokes.length}}条</text>
      </view>
    </view>
    
    <scroll-view class="today-scroll" scroll-x show-scrollbar="{{false}}">
      <view 
        class="today-card glass"
        wx:for="{{todayJokes}}"
        wx:key="id"
        bindtap="goToDetail"
        data-id="{{item.id}}"
      >
        <view class="today-color" style="background: {{item.color}}"></view>
        <text class="today-title">{{item.title}}</text>
        <view class="today-meta">
          <text class="today-cat">{{item.category}}</text>
          <text class="today-stats">{{item.likes}}{{item.hasImage ? '图' : ''}}</text>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 已看过 -->
  <view class="section seen-section" wx:if="{{seenJokes.length > 0}}">
    <view class="section-header" bindtap="toggleShowSeen">
      <view class="section-title-wrap">
        <view class="title-bar seen"></view>
        <text class="section-title seen-text">{{showSeen ? '隐藏' : '看过' + seenJokes.length + '条'}} {{showSeen ? '' : '▶'}}</text>
      </view>
    </view>
    
    <view class="joke-list" wx:if="{{showSeen}}">
      <view 
        class="joke-card fade-up seen-card"
        wx:for="{{seenJokes}}"
        wx:key="id"
        bindtap="goToDetail"
        data-id="{{item.id}}"
      >
        <view class="joke-color seen-color"></view>
        <view class="joke-body">
          <text class="joke-title">{{item.title}}</text>
          <text class="joke-preview">{{item.preview}}{{item.hasImage ? '[图]' : ''}}</text>
        </view>
        <view class="joke-right">
          <text class="joke-likes">{{item.likes}}</text>
          <view class="seen-tag">看过</view>
        </view>
      </view>
    </view>
  </view>

  <!-- 笑话库 -->
  <view class="section">
    <view class="section-header">
      <view class="section-title-wrap">
        <view class="title-bar"></view>
        <text class="section-title">{{currentCategory === '全部' ? '笑话库' : currentCategory}}</text>
      </view>
      <text class="section-count">{{filteredJokes.length}}</text>
    </view>

    <view class="skeleton-wrap" wx:if="{{loading}}">
      <view class="skeleton-card glass" wx:for="{{3}}" wx:key="*this">
        <view class="skeleton-bar skeleton"></view>
        <view class="skeleton-bar short skeleton"></view>
      </view>
    </view>

    <view class="joke-list" wx:else>
      <view 
        class="joke-card fade-up"
        wx:for="{{filteredJokes}}"
        wx:key="id"
        bindtap="goToDetail"
        data-id="{{item.id}}"
      >
        <view class="joke-color" style="background: {{item.color}}"></view>
        <view class="joke-body">
          <text class="joke-title">{{item.title}}</text>
          <text class="joke-preview">{{item.preview}}{{item.hasImage ? '[图]' : ''}}</text>
        </view>
        <view class="joke-right">
          <text class="joke-likes">{{item.likes}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 随机按钮 -->
  <view class="float-btn" bindtap="showRandom">
    <view class="float-bg gradient-btn"></view>
    <view class="float-content">
      <text class="float-icon">⚡</text>
    </view>
  </view>
</view>

<!-- 随机弹窗 -->
<view class="modal-overlay {{showModal ? 'show' : ''}}" bindtap="hideModal">
  <view class="modal glass scale-in" catchtap="preventClose">
    <view class="modal-header">
      <view class="modal-bar" style="background: {{randomJoke.color}}"></view>
      <text class="modal-cat">{{randomJoke.category}}</text>
    </view>
    
    <view class="modal-body">
      <text class="modal-title">{{randomJoke.title}}</text>
      
      <view class="modal-images" wx:if="{{randomJoke.hasImage}}">
        <image 
          class="modal-image"
          wx:for="{{randomJoke.images}}"
          wx:key="*this"
          src="{{item}}"
          mode="widthFix"
          bindtap="previewImage"
          data-url="{{item}}"
        />
      </view>
      
      <text class="modal-content">{{randomJoke.content}}</text>
    </view>
    
    <view class="modal-footer">
      <view class="modal-btn outline glass" bindtap="showRandom">
        <text class="modal-btn-text">换一个</text>
      </view>
      <view class="modal-btn primary gradient-btn" bindtap="hideModal">
        <text class="modal-btn-text-white">开心了</text>
      </view>
    </view>
  </view>
</view>
